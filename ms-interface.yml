server:
  port: 8083
  
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 5000
            readTimeout: 5000

logging:
  level:
    org.springframework.cloud.openfeign: DEBUG